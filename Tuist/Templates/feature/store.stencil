import ComposableArchitecture

public struct {{ name }}State {
    public init(
        
    ) {
        
    }
}

public enum {{ name }}Action: Equatable {}

public struct {{ name }}Environment {
    public init(
        
    ) {
        
    }
}

public let {{ name|lowercase }}Reducer = Reducer<AddRecipeState, AddRecipeAction, AddRecipeEnvironment> { state, action, environment in
    switch action {
        
    }
}
